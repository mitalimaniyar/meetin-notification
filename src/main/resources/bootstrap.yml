spring:
  application:
    name: notification
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
  profiles: 
    active:
    - dev
        
    
server:
  port: 8000

logging:
  level: 
    org.jeavio.meetin.notification: DEBUG